- project: hero-hub
  environment: aws
  description: select only database url
  secrets:
    only:
      - DATABASE_URL
    set:
       DATABASE_URL: THIS_IS_DB_URL

- project: hero-hub
  environment: vercel

  # non existing
- project: hero-hub
  environment: gdsjfkhlsd
